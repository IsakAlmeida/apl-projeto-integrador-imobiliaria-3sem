info:
  name: Listar Imóveis
  type: http
  seq: 1

http:
  method: GET
  url: "{{local}}/imoveis"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
